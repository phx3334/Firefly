---
title: 学习NFSV4
published: 2026-08-17T10:11:23+08:00
description: 学习基于kerberos5的NFSV4的原理和实际应用
image: './images/a17.avif'
tags: [计算机网络,linux,网络文件系统]
category: '计算机技术'
draft: false
lang: '中文'
---


## 前置
### /etc/exports 配置文件主要参数
- 可以配置哪些主机允许共享，共享之后的文件权限。这里区分一下这台机器对文件的权限和这台机器的用户对文件的权限，这台机器有写权限，意味着这台机器可以向服务端发送写请求，发送后服务端要看该机器上的用户映射到服务器的用户uid的权限决定能不能写。
- 选择同步落盘就是客户端对文件进行修改，落盘成功后，服务端响应成功。异步落盘就是服务端直接响应成功，攒一波数据批量落盘，异步落盘在断电或者重启后会可能丢失数据
- all_squash：把客户端**所有用户**都映射为服务端的匿名用户 nobody（UID 65534），写出来的文件属主都是 nobody，适合公共目录/不信任客户端身份的场景。可配合 `anonuid`/`anongid` 指定匿名成某个特定用户。
- root_squash：只把客户端的 **root** 映射为 nobody，普通用户的 UID 原样透传。防止远程 root 在服务端拥有文件，是**默认开启**的安全选项。
- no_subtree_check：与文件句柄有关。文件句柄是**文件系统层面**的标识、不携带路径信息，若导出的只是文件系统的一个子目录，服务端默认要"子树检查"（向上遍历路径）确认请求的文件确实在导出范围内，防止越权访问同文件系统内导出目录之外的文件。`no_subtree_check` 跳过该检查，省开销、避免重命名/移动父目录引发的 stale handle 问题；但**仅当导出的是整个文件系统/分区时**才建议使用，导出子目录时应保留检查。
### 挂载步骤：让 `cat` 能跑起来
从零到能执行 `cat /mnt/share/doc/report.pdf`，需要走通"服务端导出 → 客户端挂载"两个环节。
#### 1. 服务端：导出目录
```bash
# 编辑 /etc/exports，把 /data/share 导出给整个网段
/data/share  192.168.1.0/24(rw,sync,no_subtree_check)
# 重新读取 /etc/exports 并全部导出（-r 重新读取，-a 全部导出，-v 显示明细）
exportfs -rav
# 从客户端视角查询本机导出了哪些目录（验证上一步是否生效）
showmount -e localhost
```
#### 2. 客户端：手动挂载验证
```bash
# NFSv4 挂载。srever1为服务器主机名
mount -t nfs4 server1:/data/share /mnt/share
# 验证
ls /mnt/share
cat /mnt/share/doc/report.pdf
```
#### 3. 持久化：fstab 静态挂载
重启后想自动挂载，写入 `/etc/fstab`：
```
# 服务器:导出路径      挂载点       类型   选项                          备份 自检
server1:/data/share   /mnt/share   nfs4   defaults（包括hard，async等默认规则）,_netdev,noatime      0    0
```
- `_netdev`：等网络就绪后再挂载，避免开机时网卡未起导致挂载失败
- `noatime`：读文件不更新访问时间，减少网络写请求，NFS 场景常用
- `0 0`：NFS 不做 dump 和启动时 fsck
- 用 `mount -a` 按 fstab 内容挂载一次验证

fstab 的代价：开机即挂载，如果服务端不在线，`hard` 模式（卡住的时候会一直处于网络io）下访问会卡住进程（D 状态），甚至拖慢系统。适合**常驻共享**。  
soft模式下，固定超时重试几次后，就会放弃。
#### 4. 按需挂载：autofs
autofs 的思路是"用到才挂，空闲自动卸载"，规避 fstab 的"开机就挂"问题：
```bash
# 安装
apt install autofs

# /etc/auto.master：注册挂载点 /mnt，对应规则文件 /etc/auto.nfs，空闲 60 秒卸载
/mnt   /etc/auto.nfs   --timeout=60

# /etc/auto.nfs：定义 /mnt 下的子目录 share 对应哪个远端
share   -fstype=nfs4   server1:/data/share

# 重启 autofs 生效
systemctl restart autofs
```
之后访问 `/mnt/share/...` 时 autofs 自动触发挂载，对用户完全透明：
```bash
cat /mnt/share/doc/report.pdf    # 第一次访问自动挂载，之后正常读写
mount | grep share               # 可以看到此刻才真正挂上
```
autofs 的优势：
- 只有真正访问才发起网络挂载，不占用挂载点和资源
- 服务端故障时不影响本机开机/启动（不访问就不挂）
- 支持通配符、多服务器冗余（`-hosts` 等）高级玩法，适合**低频/临时共享**
#### 客户端fstab和服务端/etc/exports里面的sync和async
**客户端**：
- async: 写成功时，数据依旧在客户端缓存，需要积攒到一定程度再发给服务端落盘。
- sync: 如果服务端是async,那么客户端一把数据发给服务端缓存就收到写入成功响应。如果服务端是sync,那么服务端收到客户端数据后会直接落盘，然后回复响应成功。

### 文件描述符，文件句柄，inode号
这三个概念都带"文件"二字，但**分属不同层面**，容易混淆。先记一句话：
> inode 是**存储层面**的身份证，文件句柄是**网络层面**的身份证，文件描述符是**进程层面**的门票。
| 概念 | 层面 | 是什么 | 谁在用 |
|------|------|--------|--------|
| **inode 号** | 存储/文件系统 | 磁盘上每个文件/目录的唯一编号，保存元数据（属主、权限、大小、数据块位置） | 文件系统内核 |
| **文件句柄** | 网络协议（NFS） | linux内核中的一个结构体，可以让持有者不靠文件名和路径的情况下，也能准确认出是哪个文件 | NFS 客户端/服务端 |
| **文件描述符（fd）** | 进程 | 进程打开文件时内核返回的一个整数（0/1/2 是标准输入输出错误），指向内核中的打开文件表项 | 应用程序 |


## 定义
NFS（Network File System，网络文件系统）是一种分布式文件系统协议，允许一台计算机通过网络访问另一台计算机上的文件，就像访问本地磁盘一样。  
基于 C/S（客户端/服务器）架构：  
- 服务器端：把某个目录共享出去（如 /data/share），通过 /etc/exports 配置。  
- 客户端：用 mount 命令把远程目录挂载到本地路径，之后可像本地目录一样读写。  

## NFSV4过程
用一个具体场景，感受一下大致原理：客户端 `client1` 通过 NFSv4 挂载了服务端 `server1` 导出的目录 `/data/share`，本地挂载点是 `/mnt/share`。现在用户在 `client1` 上执行：
```bash
cat /mnt/share/doc/report.pdf
```

从用户敲下命令到看到内容，客户端和服务端之间发生了一系列 RPC 调用。注意：**对用户和应用程序来说，这个过程是透明的**，一切看起来都像在访问本地文件。

### 第一步：找到文件（LOOKUP）
v4 没有独立的 mount 协议，`cat` 打开文件时，客户端从挂载点根句柄开始，沿路径逐级"问路"：
```
client1                                server1
   |  PUTROOTFH（拿导出目录根句柄）        |
   |------------------------------------->|
   |      返回 /data/share 根文件句柄      |
   |<-------------------------------------|
   |  LOOKUP("doc")                       |
   |------------------------------------->|
   |      返回 doc 目录句柄                |
   |<-------------------------------------|
   |  LOOKUP("report.pdf")                |
   |------------------------------------->|
   |      返回 report.pdf 文件句柄         |
   |<-------------------------------------|
```

服务端返回的**文件句柄**是它内部给这个文件的"门牌号"，之后的读写都靠它认路，客户端不关心服务端具体怎么存。

### 第二步：打开文件（OPEN）

找到文件后，客户端发起 `OPEN`，服务端建立状态记录并返回 **stateid**：

```
client1                                server1
   |  OPEN(文件句柄, 打开方式=只读)        |
   |------------------------------------->|
   |      创建状态记录，返回 stateid       |
   |<-------------------------------------|
   |  READ(文件句柄, stateid, 偏移, 长度)  |
   |------------------------------------->|
   |                文件内容               |
   |<-------------------------------------|
```
- `stateid` 就是本次"会话"的凭证，后续 `READ`/`WRITE` 都要带着它，服务端才知道"这个客户端有资格读写这个文件"。
- 这正好解释了原理里说的：**服务端重启后 stateid 失效**，客户端必须重挂载重新拿凭证。

### 第三步：细节优化（COMPOUND + 租约）

上面的流程是"逐条 RPC"的逻辑描述。实际上为了减少网络往返，v4 会把连续操作**打包成一次 COMPOUND 调用**：

```
客户端: [LOOKUP("doc"), LOOKUP("report.pdf"), OPEN, READ] → 一次往返
服务端: 依次执行，返回全部结果
```

与此同时，`OPEN` 建立的状态进入了 **90 秒租约**。只要 `cat` 还在读文件，客户端就周期性地续租（v4.1 中由 `SEQUENCE` 顺带完成），保证状态不被服务端回收。

### 第四步：其他客户端冲突时（委托召回）

假设另一个客户端 `client2` 也要打开 `report.pdf`，而 `client1` 之前获得了该文件的**读委托**（本地缓存）：

```
client2 请求 OPEN(report.pdf)
        ↓
server1 发现文件被 client1 委托了
        ↓
server1 通过回调 CB_RECALL 通知 client1："把文件还给我"
        ↓
client1 归还委托
        ↓
server1 才处理 client2 的 OPEN
```
这正是"多客户端并发访问同一文件时短暂卡顿"的底层原因。

### 故障场景：中途网络断开 / 服务端重启
- **网络断开**：租约无法续期，约 90 秒后服务端回收 `client1` 的全部状态和锁；若用的是 `hard` 挂载，`cat` 对应的进程会停在 **D 状态**一直等待。
- **服务端重启**：重启后进入宽限期（约 90 秒），`client1` 的旧 stateid 全部失效，需要重新挂载才能继续访问。


## kerberos5在NFSv4的作用
### NFSv4里的默认sec值所带来的安全隐患
- 客户端自己上报uid/gid 可能是客户端伪造的
- 数据在网络中明文传输
### 定义
Kerberos 5 是 NFSv4 的认证/加密机制。可以解决： 你是谁（认证）、数据有没有被改（完整性）、数据是否加密（隐私）
通过修改/etc/exports文件里的sec的值可以控制对什么进行安全保护，例如只对数据完整性进行保障。
### 机制
核心一句话：**服务端不信客户端自报，只信 KDC 签发的票据。**
参与方：客户端、服务端、KDC（密钥分发中心，负责发证）。
#### 认证：你是谁（sec=krb5）
```
client               KDC                    server
  | ① 输入密码向KDC证明身份  |
  |------>|          |
  |       | ② 发TGT  |
  |<------|          |
  | ③ 凭TGT换服务票据|
  |------>|          |
  |       | ④ 用     |
  |       |   nfs/server1
  |       |   的密钥
  |       |   加密票据|
  |<------|          |
  | ⑤ 挂载时出示票据  |------>
  |       |          | ⑥ keytab解开票据，
  |       |          |    验明身份，发密钥
  |<-----------------|
```
#### 完整性：数据有没有被改（sec=krb5i）
- 每个 RPC 报文都附带一个用**双方会话密钥**算出的校验值。
- 服务端收到先验校验值：对得上才处理，被篡改的报文立刻识破丢弃。
#### 隐私：数据是否加密（sec=krb5p）
- 整个 RPC 报文体用会话密钥加密后再发送，网络抓包只能看到密文。
三者都基于同一条链：**票据证明身份 → 双方协商出会话密钥 → 会话密钥签名/加密每个 RPC**。用 `sec=` 选档位：


## NFSV4适用场景
NFSv4 适合"**多台机器共享同一份文件**"的局域网场景，但"适不适合"取决于你对一致性、安全和性能的要求：
#### 适合用 NFSv4
| 场景 | 为什么适合 |
|------|-----------|
| **用户主目录集中管理** | 用户在任何一台机器登录都是同一份家目录，配合 Kerberos 认证（krb5p）后，身份、权限、网络加密一次搞定 |
| **只读共享 / 代码库** | 源码、文档、静态资源多机只读访问，`hard+sync` 下基本无一致性问题 |
| **内部可信局域网** | 机器都在防火墙内、流量不外泄时，用 krb5i 保住完整性即可，成本低 |
| **小型集群共享配置/日志** | 集中存放配置文件、日志目录，多节点读写，NFS 开箱即用、无需改应用代码 |
#### 慎用 / 不适合 NFSv4
| 场景 | 原因 |
|------|------|
| **WAN / 跨公网** | NFS 是为低延迟局域网设计的，公网高延迟下每次 RPC 都慢；跨网明文更不安全，即便 krb5p 加密也挡不住延迟问题 |
| **多客户端高频并发写同一文件** | 委托（delegation）会被频繁召回，出现"短暂卡顿"；数据库这类强一致性场景应用自带集群方案（如 Ceph、GlusterFS）而非 NFS |
| **安全要求极高的网络** | 默认 `sec=sys` 下 uid/gid 可伪造、数据明文；必须配 Kerberos + krb5p，且要维护 KDC、keytab、票据续期，运维成本不低 |
| **单机性能敏感** | 元数据操作（小文件密集、`ls -l` 大量目录）经过网络往返，比本地文件系统慢一个量级 |
#### 一句话总结
> NFSv4 是**局域网内"共享、不追求极致性能与强一致"**的省心方案：内网共享选它，公网/强一致/高性能场景换专业分布式文件系统。
